# Code

Current status:

- can prune low ranks (w orthogonal projection) using ActSVD:
  - `cd actsvd`
  - `uv run main_low_rank_diff.py` (takes approx 20 mins)
  - `uv run chat.py` to test the jailbroken model
